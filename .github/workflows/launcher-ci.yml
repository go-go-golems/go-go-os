name: launcher-ci

on:
  push:
    branches:
      - main
      - task/**
  pull_request:

jobs:
  frontend-launcher:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint launcher app
        run: npm exec biome check apps/os-launcher

      - name: Test launcher app
        run: npm run test -w apps/os-launcher

      - name: Build launcher app
        run: npm run build -w apps/os-launcher

  go-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go-inventory-chat/go.mod

      - name: Run go tests
        working-directory: go-inventory-chat
        run: go test ./...

  launcher-smoke:
    runs-on: ubuntu-latest
    needs:
      - frontend-launcher
      - go-tests
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go-inventory-chat/go.mod

      - name: Install dependencies
        run: npm ci

      - name: Build launcher binary
        run: npm run launcher:binary:build

      - name: Run launcher smoke checks
        run: bash ./scripts/smoke-go-go-os-launcher.sh --skip-build
